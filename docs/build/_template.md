# <ID>: <Slice title>

Rules review: required | not required. Depends on: <slice ids or None>.

## Goal

One paragraph: the outcome this slice delivers and the boundary it must not cross.

## Scope

In scope, as bullets that each map to an acceptance check. Out of scope only where an implementer
would plausibly drift. Spec sections this slice implements, as `path#anchor` lines (these become the
`Spec:` trailers). For rules work: the Compendium paths read and the mechanical claims made.

## Acceptance checks

Numbered. Each names the command or headless scenario, the expected persisted state or output, and
how to read it back.

## Work log

Append-only: worktree and branch, decisions, TESTER results with artifact paths, review verdict,
commit hashes, what remains.

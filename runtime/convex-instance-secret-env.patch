--- a/crates/local_backend/src/config.rs
+++ b/crates/local_backend/src/config.rs
@@ -83,7 +83,7 @@
     pub instance_name: Option<String>,
 
     /// Instance secret for this backend.
-    #[clap(long, requires = "instance_name")]
+    #[clap(long, requires = "instance_name", env = "CONVEX_INSTANCE_SECRET", hide_env_values = true)]
     pub instance_secret: Option<String>,
 
     /// Identifier (like a user ID) to attach to any sentry
@@ -160,7 +160,7 @@
     pub instance_name: String,
 
     /// Instance secret for this backend.
-    #[clap(long)]
+    #[clap(long, env = "CONVEX_INSTANCE_SECRET", hide_env_values = true)]
     pub instance_secret: String,
 }
 

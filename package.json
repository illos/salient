{
  "name": "draw-steel-headless-playtest",
  "private": true,
  "license": "GPL-3.0-only",
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=24.12.0"
  },
  "scripts": {
    "check": "tsc --noEmit && node --test tests/*.test.ts",
    "test": "node --test tests/*.test.ts",
    "play": "node src/cli.ts",
    "demo": "node src/cli.ts demo",
    "character:sources": "node scripts/inspect-character-sources.ts"
  },
  "devDependencies": {
    "@types/node": "24.13.4",
    "typescript": "5.9.3"
  }
}
